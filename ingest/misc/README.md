# misc

Holding area for files that don't yet fit `brand/`, `website/`, or `docs/`.

Items here should eventually be re-sorted into a proper home or, if a new category emerges, promoted to its own folder. Check [`../PROCESSING-LOG.md`](../PROCESSING-LOG.md) for context on why something landed here.
